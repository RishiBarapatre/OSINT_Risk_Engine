name: osint_agent
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn
      - pydantic
      - langgraph
      - langchain
      - langchain-community
      - yfinance
      - duckduckgo-search